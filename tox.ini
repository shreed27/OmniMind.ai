[tox]
envlist = py311, lint
skipsdist = True

[testenv]
deps = -rbackend/requirements-dev.txt
commands = pytest {posargs}

[testenv:lint]
deps =
    ruff
    mypy
commands =
    ruff check backend frontend
    mypy backend

[testenv:security]
deps = pip-audit
commands = pip-audit
